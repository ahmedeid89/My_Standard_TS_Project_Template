const config = {
        verbose: true,
        testEnvironment: "jest-environment-node",
        transform: {},
        testMatch: ["**/?(*.)+(spec|test).[j]s?(x)"],
};

export default config;
